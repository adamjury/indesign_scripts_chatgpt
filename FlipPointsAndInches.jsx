// If your document measurement are in Inches it will flip it to Points. If it is anything but Points it will flip it to Inches. bounce bounce bounce.if (app.documents.length > 0) {    var doc = app.activeDocument;    if (doc.viewPreferences.horizontalMeasurementUnits == MeasurementUnits.inches) {        doc.viewPreferences.horizontalMeasurementUnits = MeasurementUnits.points;        doc.viewPreferences.verticalMeasurementUnits = MeasurementUnits.points;    } else {        doc.viewPreferences.horizontalMeasurementUnits = MeasurementUnits.inches;        doc.viewPreferences.verticalMeasurementUnits = MeasurementUnits.inches;    }}